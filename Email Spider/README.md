                              emailSpider README:
                              
How to run:
    0. Must be on Linux.
    1. Type "make" in console in the directory with the makefile.
    2. Type "./emailSpider --help" to see all of the options.
    3. Replace the example URL with the URL you are interested in.
    4. Use a search depth (-d parameter) with a depth of 3-5. 
    5. The emails should be in the file you specified with -o (emails.txt by 
       default).
